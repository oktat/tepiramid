{
  "name": "sinto-project",
  "version": "0.0.1",
  "description": "A project created by the Sinto command",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --config bs-config.json",
    "dev": "npx esbuild src/app.js --outdir=public --bundle --watch",
    "build": "npx esbuild src/app.js --outdir=dist --bundle --minify"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "browser-sync": "^3.0.2"
  }
}
